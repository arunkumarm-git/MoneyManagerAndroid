# MoneyManagerAndroid

Repository prepared for Git: added `.gitignore` and `.gitattributes`.

Quick start:

```bash
git add .gitignore .gitattributes README.md
git add -A
git commit -m "chore: add .gitignore and .gitattributes"
```

If `git` wasn't initialized, run `git init` first.
